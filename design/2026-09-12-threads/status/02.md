# 02 — Bob's onboarding fleet

RESUME HERE — DONE: fleet complete, 21/22 ticked (A6's box waits on thread 01's docs/ops.md 11g); 41 Discovered Issues logged; follow-ups landed — examples/web has vitest + the DI29 regression test wired into CI (8be169b swept, bd2d233), the hygiene gate un-redded (c02a749), web testTimeout 20s + guide cap 500 (2a4637b), and DI10 closed by a server-reported `applied` on GET /agent/charter/current (02120ba). All gates green: Go 34 pkgs exit 0 with live PG and zero skips, web 1713/1713 + typecheck + verify-package, examples/web 5/5 + typecheck + build, hygiene clean. Never pushed; main is 11 ahead of origin.
RESUME HERE — NEXT: await Kai's word on feat/project-connections, with T12/T14/T15, T-CONN-FIX and the WAHA header-name fix as the ticket list. NOTE: that branch is thread 03's, not this thread's — recorded here because the orchestrator asked; I have done no work on it and own none of it.
RESUME HERE — NEEDS FROM KAI: (1) the public repo's bare `wip` tip 88ae096, pushed by another session — tidy or leave; (2) the firsts copy, DI22 — six of seven sentences agent-authored, `first-rewrite` warns rather than explains; (3) which of DI9 / DI23 / DI36 get tickets (each triaged, none urgent, none able to bite a first real project); (4) whether the 500-word cap is meant to bind field-notes.md (894w) and glossary.md (633w), which DI35 never listed.
- Two sessions swept this thread's staged files into their own commits (DI37, DI39); 88ae096 was also pushed to the public repo.
- The Write tool turns a literal six-character NUL escape into a real NUL byte; it red-lined the hygiene gate twice today (DI40, DI41). Feedback drafted, unsent.
- Budgets: AGENTKIT_DEFAULT_DAILY_TOKENS_SOFT=50000 / _HARD=100000 (Kai's call, conservative on purpose).
- Stack down, ports 8080/8081 free, throwaway Postgres deleted, working tree clean.

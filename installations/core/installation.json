{
  "name": "core",
  "description": "Minimal, product-neutral root image: shell + filesystem essentials (coreutils, curl, jq, file, less, unzip) on the Agent Bob sandbox harness. Nothing project-specific. The root every other installation extends."
}

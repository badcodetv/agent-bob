node_modules/
# Build output. Nothing consumes the committed copy — deploy/web.Dockerfile
# runs its own `yarn build` inside the image — and every local build dirtied
# the tree, so it is generated, not tracked.
dist/
# The guide (ticket C1): generated by scripts/build-guide.mjs from
# docs/guide/*.md on every `yarn build`/`dev`/`typecheck`. Never hand-edit.
src/guide/pages.generated.ts
